spring:
  datasource:
    url: jdbc:h2:file:./gymdb
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  # Para depuração, mostra os comandos SQL no terminal
  h2:
    console:
      enabled: true